JIRA_BASE_URL=https://company.atlassian.net
JIRA_USER_EMAIL=user@example.com
JIRA_API_TOKEN=your-jira-api-token

GITHUB_TOKEN=your-github-token

FORGE_GATEWAY_URL=http://localhost:8000
POLL_INTERVAL=30

# Single-process scheduler tuning. Active tickets stay close to POLL_INTERVAL;
# dormant tickets and failures back off up to POLLER_MAX_POLL_INTERVAL.
POLLER_MAX_CONCURRENCY=8
POLLER_MAX_POLL_INTERVAL=300
POLLER_JITTER_RATIO=0.2

# Jira account ID of the Forge service account — comments from this account are ignored.
# Find it at: https://company.atlassian.net/rest/api/3/myself (log in as the service account)
FORGE_BOT_ACCOUNT_ID=

# Shared invite code for beta access. When set, the /watch endpoint (and forge-cli register)
# require this code to be provided. Leave empty to disable the check.
# BETA_INVITE_CODE=

# Path to a JSON file for persisting the watched ticket list across restarts.
# When set, the poller saves full ticket state after every change and restores it on startup.
# Leave empty (the default) to disable persistence — the watch list starts fresh each run.
# POLLER_STATE_FILE=./poller-state.json

# FORGE_BOT_GITHUB_LOGIN=forgeSmith-bot

# PRD_PROPOSALS_REPO=org/enhancement-proposals
